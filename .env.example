# ── Cloudflare Tunnel (optional) ─────────────────────────────────────────────
# Leave both unset to disable the tunnel entirely (cloudflared stays installed
# but idle). TUNNEL_ONLY only takes effect when TUNNEL_TOKEN is also set.
TUNNEL_TOKEN=
TUNNEL_ONLY=false

# ── OmniRoute ────────────────────────────────────────────────────────────────
OMNIROUTE_PORT=20128
# (add OmniRoute's own provider/token-related env vars here as needed)

# ── MimoApi ──────────────────────────────────────────────────────────────────
MIMO_PORT=3000
SERVICE_TOKENS=
USER_IDS=
XIAOMI_CHATBOT_PHS=
MIMO_API_KEY=Waguri
MIMO_CORS_ORIGIN=*

# ── zai-api / GlmApi ─────────────────────────────────────────────────────────
ZAI_PORT=3001
ZAI_AUTH_TOKEN=Waguri
ZAI_TIMEOUT=300000
ZAI_TOKEN=
ZAI_AGENT_MODE=true
ZAI_LOG_LEVEL=info
ZAI_LOG_FORMAT=text

# ── metacubexd + mihomo ──────────────────────────────────────────────────────
CONTROL_PORT=8080
CLASH_API_PORT=9090
MIXED_PORT=7890

# ── build-time source pins (used by docker-compose.yml / GH Actions) ────────
OMNIROUTE_REPO=https://github.com/diegosouzapw/OmniRoute.git#release/v3.8.50
MIMO_REPO=https://github.com/hooshidev3/mimo-ai-proxy.git#main
METACUBEXD_REPO=https://github.com/MetaCubeX/metacubexd.git#main
GLM_REPO=https://github.com/borhandarabi/GLM-Free-API.git#main
MIHOMO_VERSION=v1.19.27
